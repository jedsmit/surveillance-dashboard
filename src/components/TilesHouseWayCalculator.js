import React from 'react';

const TilesHouseWayCalculator = () => {
  //If there is a Bo, use the bo rules. never split Bo 5-10, 12, 16
  // rules for teen and day, rules for gee joon, rules for 7s, 8s, 9s
  // else, use teen/day rules
  //else, use balancing
  //account for exceptions

  return <div></div>;
};

export default TilesHouseWayCalculator;
